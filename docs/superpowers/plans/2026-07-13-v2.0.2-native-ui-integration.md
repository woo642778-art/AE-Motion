# v2.0.2 Implementation Plan

1. Add tested tool-placement metadata to the core module.
2. Add shared semantic theme and UI component helpers.
3. Redesign Extensions & Scripts with dynamic grouped sections and empty states.
4. Centralize tool controller construction.
5. Add a safe project-editor contextual tools menu.
6. Normalize Effect Picker backgrounds and collapse the hidden recommendation area.
7. Restyle preset, resource and script tables through the shared theme.
8. Add structural UI regression tests and run them before Xcode compilation.
9. Run Swift tests, metadata tests, Swift 6 isolation tests, source parse and patch-application verification.
